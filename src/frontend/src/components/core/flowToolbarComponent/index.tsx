/**
 * FlowToolbar - Previously contained Playground/API/Share buttons
 * These buttons have been moved to the header (HeaderActionButtons)
 * This component is kept for backward compatibility but renders nothing
 */
export default function FlowToolbar(): JSX.Element | null {
  // Buttons moved to header - this component is now hidden
  return null;
}
