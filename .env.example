# Description: Example of .env file
# Usage: Copy this file to .env and change the values
#        according to your needs
#        Do not commit .env file to git
#        Do not change .env.example file

# Config directory
# Directory where files, logs and database will be stored
# Example: MINERVA_CONFIG_DIR=~/.minerva
MINERVA_CONFIG_DIR=

# Save database in the config directory
# Values: true, false
# If false, the database will be saved in Minerva's root directory
# This means that the database will be deleted when Minerva is uninstalled
# and that the database will not be shared between different virtual environments
# Example: MINERVA_SAVE_DB_IN_CONFIG_DIR=true
MINERVA_SAVE_DB_IN_CONFIG_DIR=

# Database URL
# Postgres example: MINERVA_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/minerva
# SQLite example:
MINERVA_DATABASE_URL=sqlite:///./minerva.db

# Database connection retry
# Values: true, false
# If true, the database will retry to connect to the database if it fails
# Example: MINERVA_DATABASE_CONNECTION_RETRY=true
MINERVA_DATABASE_CONNECTION_RETRY=false

# Cache type
MINERVA_LANGCHAIN_CACHE=SQLiteCache

# Server host
# Example: MINERVA_HOST=127.0.0.1
MINERVA_HOST=

# Worker processes
# Example: MINERVA_WORKERS=1
MINERVA_WORKERS=

# Server port
# Example: MINERVA_PORT=7860
MINERVA_PORT=

# Logging level
# Example: MINERVA_LOG_LEVEL=critical
MINERVA_LOG_LEVEL=

# Path to the log file
# Example: MINERVA_LOG_FILE=logs/minerva.log
MINERVA_LOG_FILE=

# Path to the frontend directory containing build files
# Example: MINERVA_FRONTEND_PATH=/path/to/frontend/build/files
MINERVA_FRONTEND_PATH=

# Whether to open the browser after starting the server
# Values: true, false
# Example: MINERVA_OPEN_BROWSER=true
MINERVA_OPEN_BROWSER=

# Whether to remove API keys from the projects saved in the database
# Values: true, false
# Example: MINERVA_REMOVE_API_KEYS=false
MINERVA_REMOVE_API_KEYS=

# Whether to use RedisCache or ThreadingInMemoryCache or AsyncInMemoryCache
# Values: async, memory, redis
# Example: MINERVA_CACHE_TYPE=memory
# If you want to use redis then the following environment variables must be set:
# MINERVA_REDIS_HOST (default: localhost)
# MINERVA_REDIS_PORT (default: 6379)
# MINERVA_REDIS_DB (default: 0)
# MINERVA_REDIS_CACHE_EXPIRE (default: 3600)
MINERVA_CACHE_TYPE=

# Set AUTO_LOGIN to false if you want to disable auto login
# and use the login form to login. MINERVA_SUPERUSER and MINERVA_SUPERUSER_PASSWORD
# must be set if AUTO_LOGIN is set to false
# Values: true, false
MINERVA_AUTO_LOGIN=

# Superuser username
# Example: MINERVA_SUPERUSER=admin
MINERVA_SUPERUSER=

# Superuser password
# Example: MINERVA_SUPERUSER_PASSWORD=123456
MINERVA_SUPERUSER_PASSWORD=

# Should store environment variables in the database
# Values: true, false
MINERVA_STORE_ENVIRONMENT_VARIABLES=

# STORE_URL
# Example: MINERVA_STORE_URL=https://api.minerva.store
# MINERVA_STORE_URL=

# DOWNLOAD_WEBHOOK_URL
#
# MINERVA_DOWNLOAD_WEBHOOK_URL=

# LIKE_WEBHOOK_URL
#
# MINERVA_LIKE_WEBHOOK_URL=

# Value must finish with slash /
#BACKEND_URL=http://localhost:7860/
BACKEND_URL=